# Server Configuration
NODE_ENV=development
PORT=3000

# Session (use a strong random string in production)
SESSION_SECRET=replace-with-random-string-in-production

# Client URL (required in production for CORS)
CLIENT_URL=https://your-app.example.com

# Base URL for redirect links (used by dispatch integration)
BASE_URL=https://your-app.example.com

# Web Push Notifications (VAPID keys)
# Generate with: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=your-vapid-public-key
VAPID_PRIVATE_KEY=your-vapid-private-key
VAPID_EMAIL=your-email@example.com

# Voice Dispatcher Integration
# Generate a secure random key for API authentication
DISPATCH_API_KEY=your-secure-dispatch-api-key
